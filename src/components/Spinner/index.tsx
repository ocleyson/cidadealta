import { Spinner as StyledSpinner, SpinnerContainer } from './styles';

function Spinner() {
    return (
        <SpinnerContainer>
            <StyledSpinner />
        </SpinnerContainer>
    );
}

export default Spinner;
